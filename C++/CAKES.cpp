#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
typedef pair <int, int> times;
bool cmp(times &x, times &y)
{
    if(x.)
}
void solve()
{
    int n;
    cin >> n;
    vector <times> arr(n+1);
    for(int i = 1;i <= n; i++)
        cin >> arr[i].first >> arr[i].second;
    
}
int main()
{
    return 0;
}
